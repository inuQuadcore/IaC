# v2.6.1 — 보안: .claude/ gitignore 처리

**날짜:** 2026-03-18
**커밋:** `9ae62b2` Security: .claude/ gitignore 추가 및 tracking 제거

---

## 🔒 Security

- `.claude/settings.local.json`에 서버 IP, 인프라 정보 등 민감 정보 포함 확인
- git tracking 제거 (`git rm --cached .claude/settings.local.json`)
- `.gitignore`에 `.claude/` 디렉토리 추가

---

## ✏️ Changed

```gitignore
# .gitignore 추가 항목
.claude/
```

---

## 참고

git history에는 해당 파일이 이전 커밋(`c499eeb`, `c8d4175`)에 잔류.
레포를 public으로 전환할 경우 `git filter-repo`로 히스토리 완전 제거 권장.
