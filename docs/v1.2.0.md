# v1.2.0 — 백엔드 서버 스펙 업그레이드 + EIP 추가

**날짜:** 2026-02-05
**커밋:** `9532433` Feat: backend server upgrade

---

## 🆕 Added

- **Elastic IP** `everybuddy-backend-eip` — 고정 IP 할당 (`18.136.148.40`)
- **EIP Association** — EIP ↔ EC2 `everybuddy-backend` 연결

## ✏️ Changed

- **EC2** `everybuddy-backend` 인스턴스 타입 변경
  - `t3.micro` (1 vCPU, 1GB RAM) → `t3.small` (2 vCPU, 2GB RAM)
  - Spring Boot 운영 환경 메모리 부족 문제 해결

---

## 변경 이유

t3.micro에서 Spring Boot 실행 시 메모리 부족으로 인한 성능 저하 발생.
EIP 추가로 인스턴스 재시작 시에도 IP 고정 유지.
